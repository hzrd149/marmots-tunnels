# @libs/qrcode is published on JSR (used by the app); marmot-ts also relies on this.
@jsr:registry=https://npm.jsr.io

# Don't fail the install when an unapproved dependency build script is skipped
# (esbuild, pulled in transitively for docs tooling we never run).
strict-dep-builds=false
